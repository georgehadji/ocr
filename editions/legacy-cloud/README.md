# Legacy Cloud Edition (migrated)

This directory contains the original Cloud Edition codebase, preserved for
reference. The active Cloud Edition composition root lives in
`editions/server/`.

**Migration status:** The shared core (`packages/omniocr/`) now provides all
business logic. Edition-specific composition roots live in `editions/`.
